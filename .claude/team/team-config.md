# Team config — Network Management System

Generated by `/init-team`. This file + the overlays in `./overlays/` are the ONLY source of project facts for the virtual team. Commit this whole `.claude/team/` tree with the code.

## 1. Project
- **Name:** Network Management System
- **Description:** Enterprise NMS — LibreNMS as headless collection/alerting engine (SNMP discovery, polling, syslog/traps, alert rules) behind a custom React/Next.js operations UI, with unified OIDC SSO across the custom UI and the native LibreNMS admin UI. Founding spec: `docs/requirements/nms-platform-foundation.md`. Architecture reference: `LibreNMS_Custom_UI_Architecture.md`.
- **Repo root marker:** this file lives at `.claude/team/team-config.md` relative to repo root. All paths below are repo-relative.

## 2. Mode & roster
- **Mode:** dev
- **Roster (dev):** technical-architect, developer, code-reviewer, tester
- **Orchestrator:** jarvis (always)
- **Model policy:** ALL team agents run on Opus. Jarvis must pass `model: opus` when spawning any role agent (developer, tester, code-reviewer, technical-architect). Plugin agent frontmatter also pinned to opus locally; re-apply after plugin updates if frontmatter reverts.
- Roster overrides (add/remove roles only with a documented reason): none

## 3. Protected branches & commits
- **Protected branch(es):** main
- **Feature branch naming:** feature/<ticket-id>-short-description (lowercase, hyphen-separated)
- **Commit convention:** Conventional Commits
- **Commit policy:** manual — agents never commit or push; human reviews and commits after G4

## 4. Gates
Standard gates G1–G4 per the `team-protocol` skill. Project-specific ADDITIONAL gates (may add, never remove):
none

## 5. Document paths
- Requirements: docs/requirements/
- Design docs (dev mode): docs/design/
- Plans: docs/plans/
- ADRs (dev mode): docs/adr/

## 6. Memory paths
Per the `memory-methodology` skill. Default (recommended): `.claude/team/memory/`. Override: default

## 7. Technology stack (RESOLVED at G1 — 2026-08-09, human decision on OQ-1)
The project is **polyglot by construction**. The Java/Maven toolchain assumed at init time is RETIRED — it was an init-time default, not a project decision.

- **Custom operations UI:** TypeScript + React via **Next.js**. Team-owned code.
- **API gateway / BFF:** TypeScript on **Node.js**. Team-owned code. Holds all LibreNMS API tokens and TSDB credentials server-side; nothing credential-bearing reaches the browser (approved BFF deviation from the architecture reference's global-token Nginx pattern — see requirement doc FR-08/NFR-09/OQ-6).
- **Collection engine:** **LibreNMS (PHP/Laravel)** — deployed and configured as-is, NEVER modified (requirement doc FR-07). Not team-authored code; treated as a platform dependency.
- **Toolchain verified on this machine (2026-08-09):** Node **v24.16.0** — verified; npm **11.13.0** — verified; git 2.49.0 — verified.
- **PHP:** NOT installed on this machine (`php: command not found`) — see open question below. Not required for Node-side build/test work.
- **Dependency policy:** versions pinned by the repo's manifests (package.json + package-lock.json). Any version change requires explicit plan approval — an unrequested version change is a defect, not initiative.
- **`@babel/parser@7.29.8` added 2026-08-09 (declared, pinned, flagged — pending human approval at G2.5).** Reason: `typescript@7` is the native Go rewrite and **no longer exposes a JS parse API** (`ts.createSourceFile` is gone), so the plan's assumption that "typescript is already a devDep, so an AST parse costs no new dependency" is **dead**. `@babel/parser` was already in the lockfile transitively via vitest; it is now declared explicitly at the locked version. Record the outcome in plan reconciliation.
- **`concurrently` is now unused** (the `dev` fan-out was removed per review finding 15). Removing it is a version change requiring plan approval — the Developer correctly left it in place. Queued for plan reconciliation.

### Build / test commands (Node) — VERIFIED 2026-08-09 against the real scaffold
Confirmed by the Developer's Task 1–3 run on branch `feature/nms-1-nms-platform-foundation` and independently re-run by Jarvis:
- Install (CI-deterministic): `npm ci` — **verified: yes** (196 packages, 0 vulnerabilities)
- Build: `npm run build` — **verified: yes**
- Full test run: `npm test` — **verified: yes** — **276 tests, 0 failures, 0 skipped, 0 todo** (177 vitest: **168 `@nms/bff` + 9 `@nms/shared`**; plus **99 `node:test`** guard tests). Re-verified by Jarvis at `24f0d19` on 2026-08-09.
  - *History of this number:* 222 (wrong — omitted the 9 shared tests) → 231 (`b0cd4b9`) → 244 (`625f710`) → 258 (`3e19165`) → 273 (`e97dfd3`) → **276** (`24f0d19`, +3 drift-guard tests). **Always run the command and quote the per-package/per-runner breakdown**; a bare total hides which suites were counted.
- Local run: **`npm run dev` WORKS** — it now aliases `dev:bff` and starts the BFF on **:4000**. It requires a valid env (fail-fast config will refuse to start otherwise — that is correct NFR-29 behaviour, not a fault). The earlier "npm run dev is BROKEN" note is **superseded**.
- Lint: `npm run lint` now runs the workspace `lint` scripts (`--if-present`) **and** `lint:deps`, so it is **no longer a total no-op** — `lint:deps` genuinely runs. **But ESLint is still NOT scaffolded**, so no JS/TS style/correctness linting happens. Do not describe `npm run lint` as a full lint gate; it is a dependency-rule gate only.
- **ONE command now covers everything.** The deps-guard tests were folded in via a `test:deps` script (`npm test` = `--workspaces --if-present && npm run test:deps`), so the earlier "you must run BOTH commands or the NFR-09/AC-F#31 guard goes unexercised" caveat is **RESOLVED**. `node --test scripts/check-workspace-deps.test.mjs` still works standalone if you want the guard alone.
- Workspace dependency guard: `npm run lint:deps` — **verified: yes**, and **independently re-probed by Jarvis at `ed82337` with 11 cases: 9 violation forms all exit 1** (outside `src/`, `.jsx`, dynamic `import()`, side-effect import, deep specifier, `require()` in `.cts`, re-export, type-only import, `lib/` dir) **and 2 false-positive cases correctly exit 0** (import-like text in a comment and in a string). The guard parses an AST rather than matching text.
- **FAIL-CLOSED semantics (added at `b0cd4b9`, review finding 20).** The guard no longer requires a string literal: expression-free template literals are resolved, and **any specifier that cannot be statically resolved is reported as a violation** rather than skipped — scoped to rule-carrying packages, so `bff` may use dynamic imports while `web` may not. Jarvis verified at `b0cd4b9`: template literal / `require` template / template+expression / string concatenation / `Identifier` / conditional / zero-arg `require()` all exit 1; a dynamic specifier in `bff` exits 0. **Mutation-tested by Jarvis:** disabling the unresolvable branch fails 10 of 99 guard tests, so the coverage is not vacuous.
- The file `scripts/check-workspace-deps.mjs:28-30` declares its own fail-closed invariant. **Four sequential fail-opens have been found in this one file.** Any change to it must be re-probed on the NODE-TYPE axis (not just new string values) and checked against that invariant.
- Type check: `npm run typecheck` — **verified: yes**, but the command CHANGED to **`tsc -b`** (the plan's `tsc -b --noEmit` is incompatible with composite project references — TS6310). A root `tsconfig.json` was also required and was not in the plan's file list.


Other available scripts: `test:unit`, `test:integration`, `test:coverage`, `dev` (concurrently bff+web), `dev:bff`, `dev:web`, `sim`.
Local run: **RESOLVED 2026-08-09** — `npm run dev` works (aliases `dev:bff`, BFF :4000). `npm run sim` awaits the simulator workspace (Task 12).

## 8. Deployment target (RESOLVED 2026-08-09 — OQ-22/23/24/25)
- **Target host:** `10.121.77.206`. LibreNMS + supporting services are **installed by this project** on it (requirement doc FR-54..58) — not an assumed environment.
- **Install method:** official **Docker Compose** distribution (OQ-24 resolved). **TSDB: TimescaleDB** (OQ-3 resolved). **Keycloak is co-hosted** on the same server (OQ-25 resolved), which also answers OQ-2 — but ONLY if discovered specs exceed the ADR 0008 floor of **4 vCPU / 8 GB**; at or below it, STOP and flag to the human before installing Keycloak.
- **DEPLOYMENT IS AUTHORIZED FOR AGENTS** (human decision 2026-08-09: *"use the credentials to deploy the solution"*). This overrides the general team-protocol §5 posture for THIS host only. Guardrails are mandatory:
  1. **Credential hygiene (Critical).** SSH credentials are in the **gitignored repo-root `Credentials.md`**. Read it ONLY to establish the connection. NEVER copy its contents into any doc, config, log, artifact, commit, handoff, status file, or terminal echo — reference the path only. A leak is a Critical finding (team-protocol §6).
  2. **Evidence per step.** Record expected-vs-actual output for each Task 0.x step under `.claude/team/artifacts/nms-platform-foundation/`, secrets redacted.
  3. **Stop before destruction.** Disk wipes, OS changes, or removing/reconfiguring pre-existing services → STOP and confirm with the human.
  4. **Stop if shared.** If the host carries unrelated production services → STOP and report before installing anything.
  5. **Facts first.** OS, Docker availability, CPU/RAM/disk, and sudo are UNKNOWN. SSH fact-gathering (Task 0.1) precedes every install action and gates the install path.
- **SSH:** Windows 11 OpenSSH client is available. Bootstrap with password auth, then install a project SSH key for non-interactive automation.
- **DEPLOYMENT SHAPE DECIDED 2026-08-09 (human option 3 after Task 0.1 STOP):** minimal-footprint **rootless Podman 5.6.0** POC, **no sudo**, with explicit human consent to **co-tenancy** with the host's existing workloads. Install branch **0.4c** (new) — NOT 0.4a Compose, NOT 0.4b native. Pending the Architect's doc amendment before deployment resumes.
  - **Unprivileged ports only:** UI/API **8080**/**8443**, SNMP traps **1162/udp**, syslog **1514/udp**. Our simulators target the high ports. **Consequence:** real devices cannot send traps to 1162 or syslog to 1514 without a privileged redirect — a documented POC limitation, and FR-56 (which specifies 161/162/514) needs a human wording decision.
  - **Storage under the account home** (e.g. `~/nms`). **NEVER `/opt/airlinq`** — another project's volume. Budget: 35 GB free on `/`.
  - **POC scale only (tens of devices).** NFR-01's >5,000-device target is explicitly NOT demonstrated here and needs a dedicated host later.
  - **OFF-LIMITS on that host:** Kafka, ZooKeeper, ports **8077** and **5000**, Samba. Zero modification of existing files, processes, crons, shell profiles, or services. Any unavoidable addition is append-only with a clear comment.
  - **SELinux Enforcing:** `:Z` volume labels; no policy changes.
  - **Standing aborts during deploy:** anything needing sudo → STOP; interference with existing workloads → ABORT; disk above 80% → STOP.
- **HUMAN DECISIONS 2026-08-09 (deployment go-ahead — 7 answers):**
  1. **G3 APPROVED.**
  2. **`querystring.parse.rangeError` RATIFIED as 400 VALIDATION_ERROR.**
  3. **Drift-guard test AUTHORIZED** (one test, through the middleware, single exported pattern).
  4. **subuid/subgid:** the human will ask their admin for the root operation **if needed**. **Recon never actually captured `/etc/subuid` / `/etc/subgid` contents** — a read-only check comes FIRST; many EL9 installs pre-populate ranges, in which case no admin is needed and deployment proceeds immediately.
  5. **VM snapshot: WAIVED by the human.** No pre-flight snapshot exists. Consequence recorded: there is **no rollback-to-image safety net** — every change must be individually reversible and confined to `~/nms`, and the "stop before destruction" guardrail is now the ONLY protection against an irreversible mistake. Treat it as load-bearing.
  6. **TLS: self-signed certificate**, generated during deploy. Trust caveat must be documented (browsers and any API client will warn; not a production posture).
  7. **Scope: simulator-only ingestion POC ACCEPTED** for this phase, and **FR-56a scoped exception APPROVED** (see requirement doc §5.8). FR-56 (161/162/514) stands unchanged as the production requirement.
- **GUARDRAIL AMENDED AND DEPLOYMENT RESUMED — human approved option 1 on 2026-08-09.** This SUPERSEDES the "NEVER `/opt/airlinq`" prohibition recorded earlier. The amended rules, which are now the operative ones:
  1. **Storage location ACCEPTED:** `~/nms` = **`/opt/airlinq/aqaillm/nms`**. Permitted because it is inside our own `$HOME`.
  2. **The prohibition is now scoped, not absolute:** *never write outside our own `$HOME` (`/opt/airlinq/aqaillm`); never touch `/opt/airlinq/Thunder_Sprint_1` or ANY path not owned by `aqaillm`.* The original clause's intent — do not touch the third party's data, keep our footprint enumerable and reversible — is preserved exactly; only the path wording changed.
  3. **ALL disk checks switch to `df -h /opt/airlinq`** (device `vg_opt-lv_opt`, the filesystem that actually fills). `df -h /` is no longer a valid guardrail on this host. The **80% abort now measures the right volume** — baseline 38% used, 94 GB free.
  4. **PRECONDITIONS OF FIRST START (not deferrable):** TimescaleDB retention policy AND container log caps must be configured **before** the stack is first started. Rationale: this volume also holds the third party's Kafka log directory and the VM snapshot is waived, so an unbounded database filling it has no rollback.
  5. **Shared-UID hazards STAND:** **no `loginctl disable-linger`**, **no blanket `podman system prune`**, never stop/modify/remove the account's pre-existing containers or images (`amx-mcp-server` etc.), and the account's own services on **:8077** (`cli/serve.py`) and **:5000** (`local_rag`) must keep running. Off-limits listeners unchanged: **9092, 2181, 8077, 5000**.
  6. **subuid/subgid CONFIRMED PRESENT** (`aqaillm:296608:65536`) — no admin round-trip. **SSH key install may now proceed** (append-only, clearly commented) per the earlier option-3 consent, so password auth can be retired.
- **Superseded record (kept as history) — DEPLOYMENT STOPPED 2026-08-09 at Task 0.4c Step 1/6 — GUARDRAIL CONTRADICTION, human decision required.**
  - **subuid/subgid blocker was FALSE and is CLEARED.** Measured: `/etc/subuid` and `/etc/subgid` both contain `aqaillm:296608:65536`; `podman unshare id` maps to root in the namespace. **No admin action is needed; do NOT send the `usermod --add-subuids` one-liner.** Task 0.1 had escalated it from inference without reading the files. This account had already run rootless Podman containers before we arrived.
  - **NEW blocker: `$HOME` for `aqaillm` is `/opt/airlinq/aqaillm`** (`/home/aqaillm` does not exist; `/home` is not writable). Therefore **`~/nms` IS `/opt/airlinq/aqaillm/nms`** — the two human guardrails "storage under `~/nms`" and "NEVER `/opt/airlinq`" name the SAME directory on this host. No path satisfies both.
  - **Consequences:** (1) every disk guardrail in Task 0.4c measures the WRONG filesystem — `$HOME` is on `vg_opt-lv_opt` (149G, 94G free, 38%), not `cs-root`, so `df -h /` would stay at 29% however much we wrote and the 80% abort would never fire; (2) that filesystem also holds the third party's **Kafka log directory** (`/opt/airlinq/Thunder_Sprint_1/kafka_2.13-3.9.1/logs`) — an unbounded TimescaleDB filling it is exactly what co-tenancy guardrails exist to prevent, and with the snapshot waived there is no rollback; (3) `/` is not an alternative (only `/tmp`/`/var/tmp` writable; Podman graphroot already pinned under `$HOME` alongside 349 MB of another workload's images).
  - **Options for the human:** (1) accept `~/nms` = `/opt/airlinq/aqaillm/nms`, amend the guardrail to "never write outside `$HOME`; never touch `Thunder_Sprint_1` or any non-`aqaillm` path", switch all disk checks to `df -h /opt/airlinq`, and make TimescaleDB retention + container log caps a PRECONDITION of first start; (2) different host or dedicated volume; (3) abort this phase. Deploy agent and Jarvis both recommend (1) — a recommendation, NOT an approval.
  - **Host integrity confirmed:** nothing created/modified/started/stopped, no sudo, off-limits listener count 4 at every probe (Kafka 9092, ZooKeeper 2181, 8077, 5000), disk `/` 29%→29% and `/opt/airlinq` 38%→38%. **SSH key NOT installed** (deliberately — a durable write to a shared host at the start of a run that then stopped); password auth still in use; pre-existing `authorized_keys` line count recorded as 1.
  - **`pasta` is ALREADY the default rootless forwarder** (`passt` installed) — the source-IP attribution problem is pre-solved and plan Step 5's `containers.conf` append is unnecessary (the file does not exist). Still requires empirical proof at Step 10(3), not `podman info`.
  - **NEW RISKS for the Architect:** we share a UID with two of the account's own long-running services (`cli/serve.py` :8077, `local_rag/src/app.py` :5000), so the plan's teardown step `loginctl disable-linger` is **NOT safe here**; and any blanket `podman system prune` would destroy this account's pre-existing `amx-mcp-server` images.
- **NO URLS EXIST.** Nothing is listening on 8443/8080/1162/1514; `LIBRENMS_BASE_URL` is unset; Tasks 0.5/0.6 not reached; FR-58 unverified. No URL or result was fabricated.
- **DEPLOYMENT AUTHORIZED TO EXECUTE** per amended plan Tasks 0.2 → 0.6 on the **0.4c rootless-Podman** branch, once the subuid/subgid check passes. Deliverable: a working deployment **and the URL(s)**. Expected shape `https://10.121.77.206:8443`; the completion report must confirm the actual URLs for the custom UI/BFF gateway, the LibreNMS native UI via the gateway, and Keycloak.

## 9. Test environment (RESOLVED at G1 — human decision on OQ-17)
- Verification uses **DUMMY POLLERS / simulated devices** rather than production hardware: simulators exposing **SNMP** and **TR-069** so the platform has something real to poll.
- **TR-069 is a new protocol dimension** not present in the architecture reference. LibreNMS is SNMP-centric; TR-069 (CWMP) is not a native LibreNMS collection protocol and likely implies a separate ACS component or an adapter feeding the system. This is an explicit DESIGN QUESTION for the Technical Architect (requirement doc OQ-21) — it must NOT be treated as silently in scope.

## 10. Open questions
- RESOLVED 2026-08-09: health-check contract fixed by the design (`/health` liveness, no dependency calls; `/ready` dependency-aware, 503 when a dependency is down; exact JSON per design §3.3) and verified at run-1. `npm run dev` works (aliases `dev:bff`). Remaining: `npm run sim` awaits Task 12.
- RESOLVED 2026-08-09 (artifact collection — human adopted the default): every verification run writes to **`.claude/team/artifacts/<work-item>/run-<n>/`** (n = findings-loop iteration) in the PRIMARY checkout, never a worktree. Contents: **test-framework reports** (vitest + `node:test` output), **BFF logs**, and **terminal command captures** (exact command + output). **Screenshots via the Playwright MCP once a UI exists** (none yet — Task 10). `results.md` carries the per-case table and MUST end with `status: complete`. Secrets and credentials are redacted in all captures.
- RESOLVED 2026-08-09 (OQ-2): Keycloak is stood up co-hosted on `10.121.77.206` (per OQ-25). A POC-grade IdP is explicitly NOT a production identity solution and would be replaced by a corporate IdP later.
- RESOLVED 2026-08-09 (OQ-3): **TimescaleDB**. Isolated behind the design's `MetricsReader` port.
- RESOLVED 2026-08-09 (OQ-11): vendors selected by the Architect and accepted in **ADR 0007 revision 2 — Cambium Networks + Ubiquiti**, with MIB-inference pairing plus a registry override. **Phase 2 work — build nothing for it in Phase 0/1.** Revisit if the real estate differs.
- RESOLVED 2026-08-09 (OQ-12): flapping = **≥3 state transitions within 5 minutes**, computed in the BFF (not a native LibreNMS state).
- RESOLVED 2026-08-09 (OQ-14): Architect's proposal accepted. Note NFR-05 is measured from LibreNMS *recording* a change, so operator-perceived latency = NFR-05 + polling interval.
- RESOLVED 2026-08-09 (OQ-21): **simulator-tolerance only** (ADR 0004 option a). Real TR-069 monitoring is a FUTURE SEPARATE WORK ITEM with its own G1 — not this one.
- RESOLVED 2026-08-09 (OQ-22): LibreNMS runs on a **remote server the human will provide**, and **installing LibreNMS on that server is IN SCOPE** for this work item (requirement doc FR-54..58, §5.8) — MariaDB, RRDCached, Redis, dispatcher/poller, SNMP prerequisites, firewall/ports, and the SSO config hook. `LIBRENMS_BASE_URL` will point at that server. PHP absence on this dev machine is therefore moot for the engine.
- RESOLVED 2026-08-09: OQ-23 server is `10.121.77.206`, credentials in gitignored `Credentials.md`, deployment authorized for agents with guardrails (see §8). OQ-24 = Docker Compose. OQ-25 = Keycloak co-hosted (floor check applies), which resolves OQ-2.
- OPEN (discovered at runtime, not human-answerable): server OS + version, Docker/Compose availability, CPU/RAM/disk, sudo access, and whether the host is shared. Task 0.1 SSH fact-gathering establishes these; they gate the install path and the Keycloak co-host decision.
- RESOLVED 2026-08-09: stack is Node/TypeScript + React/Next.js (was: Java/Maven). The former "Maven not installed" open question is RETIRED — Maven is not part of this project.
- Note: repo is greenfield — no package.json exists yet; build/test commands in §7 assume the first feature scaffolds the Node project.
