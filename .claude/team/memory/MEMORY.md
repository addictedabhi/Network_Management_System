# Team memory — Network Management System

Curated index. One line per learning; detail in `learnings/`. Jarvis is the only writer.

- 2026-08-09 — **Stack is Node/TypeScript, never Java.** `/init-team` seeded a Java/Maven toolchain by default; the real project is React/Next.js + Node BFF with LibreNMS (PHP) as an unmodified engine. team-config §7 and all four overlays were corrected at G1. If any agent proposes Maven/`mvn`/`src/main/java`, it is reading a retired fact — stop and re-read team-config.
- 2026-08-09 — **The architecture reference contains two security anti-patterns; do not implement them as written.** Its Nginx sample injects a *global* LibreNMS API token into a browser-reachable `/api/v0/` proxy, and it suggests the browser query InfluxDB directly. Both are formally rejected in ADR 0002 and replaced by a server-side BFF (FR-08/FR-46/NFR-09, CON-6). Treat the reference as an architectural sketch to be reviewed, not a spec to be transcribed.
- 2026-08-09 — **LibreNMS core is never modified** (FR-07, goal G-6). Any design needing a core patch is a design failure — escalate instead, because core patches turn every LibreNMS security release into a merge exercise.
- 2026-08-09 — **Simulator-based verification has a trap: a simulator that always answers cannot test the "metric unavailable" path.** FR-24/FR-52 require the harness to withhold OIDs on demand, because rendering an absent RSSI as `0` on an RF dashboard is indistinguishable from a dead link.
- 2026-08-09 — **TR-069 is not a LibreNMS protocol.** It entered scope via the test-environment decision, not the architecture doc. LibreNMS is SNMP-centric; TR-069/CWMP implies a separate ACS. Architect recommends simulator-tolerance only (ADR 0004); real TR-069 monitoring would be a separate work item with its own G1, and TR-069 *writes* would collide with OOS-2 (no config management).
- 2026-08-09 — **Prefer structural enforcement over reviewer vigilance.** The strongest design choices here make requirements impossible to violate rather than merely forbidden: a `lint:deps` script failing CI if `web` imports `bff` (NFR-09/AC-F#31), a BFF-mediated opaque Redis session so no token can reach the browser (FR-12), and a `MetricValue` discriminated union whose absent case has no numeric slot (FR-24). Reach for this pattern first.
- 2026-08-09 — **A resolved open question can enlarge scope, not just settle a value.** OQ-22 looked like "which URL?" and turned into a systems-administration work package (FR-54..58) once the human confirmed LibreNMS must be installed on their server. Re-check scope whenever an answer lands, and re-issue affected FRs rather than absorbing the work silently.
- 2026-08-09 — **Verification "by design" is not verification.** ASM-1 (LibreNMS API covers every FR) was confirmed by reading docs, not by calling a live API. The plan therefore carries a dedicated real-API verification task, and its riskiest rows are server-side pagination (FR-38), filters (FR-31/37), and acknowledger identity (FR-32). Watch for agents treating a documented API as a verified one.
- 2026-08-09 — **Watch latency requirements worded against the wrong event.** NFR-05 says ≤10 s from LibreNMS *recording* a change, which is very different from the change *occurring* when polling every 5 minutes. Ambiguity of this kind silently converts an unachievable target into an achievable one (or vice versa) — pin the reference event.
- 2026-08-09 — **A security guard that can fail OPEN is worse than no guard, because it is trusted.** The `lint:deps` control enforcing "web never imports bff" (NFR-09/AC-F#31) skipped its source scan entirely when a package manifest was missing or malformed — so a real violation passed with exit 0. Found by the Developer during Task 1, fixed with regression tests on real temp-dir fixtures. Rule: every structural guard needs a NEGATIVE probe proving it fails when it should; exit 0 from an unproven guard means nothing. Re-probe `scripts/check-workspace-deps.mjs` after any change.
- 2026-08-09 — **`npm run lint` is currently a no-op** (`--if-present`, no workspace defines `lint`, ESLint never scaffolded — the plan named the script without the dependency). It exits 0 and proves nothing. Never cite it as a passing lint gate until ESLint is actually scaffolded.
- 2026-08-09 — **Plan-specified commands can be wrong in ways only execution reveals:** `tsc -b --noEmit` is invalid with composite project references (TS6310), and `node --experimental-strip-types` cannot resolve NodeNext `.js` specifiers. Both were in the approved plan and both failed on first run. Approved plans are not verified plans — expect the maker to hit and report this class of defect, and reconcile the plan text afterwards.
- 2026-08-09 — **Closing a fail-open instance is not closing the class.** The `lint:deps` guard was fixed for missing/malformed manifests, then failed open three more ways at G2.5 review: it only walked `packages/<pkg>/src` (blind to Next.js `app/`, `pages/`, package-root files), its extension allowlist missed `.jsx/.cts/.cjs`, and its import regex missed dynamic `import()` and side-effect imports. Root cause: it reasoned about *text in a subdirectory* instead of *the module graph*. Lesson: when a guard fails open once, ask what CLASS of input it mis-handles and fix the abstraction (AST parse), not the symptom (another regex).
- 2026-08-09 — **Placing a crash on the failure path doubles its cost.** `redact()` stack-overflowed on circular objects and was reachable from the error handler, so a handled error became a process crash AND destroyed the log explaining it. Audit failure-path code (error handlers, loggers, health checks) more harshly than happy-path code.
- 2026-08-09 — **Anything that cannot fail is not a check.** Hardcoded always-OK health placeholders and a no-op `npm run lint` both report success while proving nothing — the same defect class as the fail-open guard. Unimplemented checks must fail CLOSED or be explicitly absent; never fabricate OK.
- 2026-08-09 — **Jarvis's copy of an agent report can be lost (0-byte transcript) even when the report itself survived elsewhere.** At G2.5 the reviewer's 19 findings reached the team intact but Jarvis's transcript copy was empty, so Jarvis relayed a degraded reconstruction and told the Developer to "re-derive" 7 items that were in fact fully specified. Two mitigations: (a) verifier roles MUST write findings to `.claude/team/artifacts/<work-item>/` as a file rather than relying on the returned message; (b) when a report looks truncated, say so and re-verify independently before relaying — Jarvis reproduced the 3 Criticals by live probe, which is why those survived the loss intact. Never present a reconstruction as verbatim.
- 2026-08-09 — **`typescript@7` is the native Go rewrite and exposes NO JavaScript parse API** (`ts.createSourceFile` is gone). Any plan step that assumes "typescript is already installed, so AST work is free" is invalid on TS7. Use `@babel/parser` (commonly already in the lockfile via vitest) or another parser, declared and pinned explicitly.
- 2026-08-09 — **A guard must be measured on BOTH axes.** The rebuilt dependency guard was verified with 9 violation forms that must fail (outside `src/`, `.jsx`, dynamic `import()`, side-effect, deep specifier, `require()`, re-export, type-only, `lib/`) AND 2 import-like-text cases that must pass (comment, string). False negatives let violations through; false positives get the guard socially bypassed. Test both directions or the control is unproven.
- 2026-08-09 — **The maker found the original defect was worse than the reviewer reported** (7 of 10 probes escaped, not 3) and found an extra unreported one: `ROLE_MAP` — authorization config — accepted `123`, `[1,2]`, and `null` before shape validation was added (`env.ts:84-91`). Encourage makers to report severity ESCALATIONS; a fix round is the cheapest moment to find siblings of the same defect class.
- 2026-08-09 — **Test the AXIS, not more values on the same axis.** The dependency guard's third fail-open (finding 20) survived 64 tests and 11 Jarvis probes because every one used a **string-literal** specifier. Template literals, `BinaryExpression`, and `Identifier` specifiers were never exercised — a whole node-type axis untested while the value axis was saturated. When a control has an input taxonomy (node types, encodings, protocols), enumerate the taxonomy; adding a 12th value on the axis you already cover proves nothing new.
- 2026-08-09 — **Check code against the safety invariant it declares in its own comments.** `scripts/check-workspace-deps.mjs:28-30` states "every error path here must produce a VIOLATION, never a silent skip" — and `pushString` plus two `walk()` catch blocks did exactly that silent skip. The comment was right; the code disagreed. A stated invariant is a free test oracle: grep for it, then verify every error path honours it.
- 2026-08-09 — **When one fail-open is found, assume siblings until proven otherwise.** This single file yielded three sequential fail-opens (manifest parse → subdirectory/extension/import-form → non-literal specifier), each surfacing only after the prior fix. Budget for a class sweep of every error path in the file, not a point fix; and have the maker report unasked-for siblings.
- 2026-08-09 — **Fail-closed beats enumerate-and-catch.** The dependency guard's four fail-opens all came from enumerating *allowed* shapes and silently ignoring the rest. The durable fix inverted the default: classify every specifier as `resolved | unresolvable`, and treat unresolvable as a VIOLATION. Scope it (rule-carrying packages only) so it is not blanket-noise — `bff` may use dynamic imports, `web` may not — and emit a message naming the node type so the failure is actionable. Prefer this shape for any allowlist-style control.
- 2026-08-09 — **Mutation testing is the cheap proof that tests are not vacuous.** Claim: "the tests cover the fail-closed branch." Verification: disable the branch and confirm the suite fails — 10 of 99 guard tests died. A passing suite proves nothing about a branch until you have watched the suite fail without it. Also note the maker did this unprompted, and Jarvis reproduced it independently.
- 2026-08-09 — **A representational gap can be the root cause.** `extractSpecifiers` returned `string[]`, a type that literally cannot express "a specifier I could not resolve" — so the only options were a fake string or silence, and silence won. Widening the type to `{kind, value|nodeType}[]` made the failure case representable and therefore handleable. When a control keeps failing open, check whether its data types can even express failure.
- 2026-08-09 — **Windows blocks symlink creation, which silently disables symlink tests.** Instead of skipping (0 coverage while looking green), inject a `readDir` seam so both branches execute on any OS. A skipped test is an untested path wearing a passing badge; verify `skipped: 0`, not just `fail: 0`.
- 2026-08-09 — **Distinguish a finding from an observation.** At iteration 3 the reviewer found transitive laundering (unruled package imports `bff`, re-exported into `web`) and recorded it as an OBSERVATION, not a finding: it is inherent to the direct-edge rule model, pre-existing, and a design decision rather than a defect. That judgement kept the loop closable while still capturing the risk (reconciliation item 10). Reviewers who cannot make this distinction either block forever or lose real risks.
- 2026-08-09 — **A `default:` branch is where fail-open hides.** The reviewer's exit criterion at iteration 3 was proving the guard's `default:` case declines classification while recursion continues, so no third outcome is possible. When auditing an allowlist-style control, enumerate its outcomes and prove there is no unhandled path — 4 mutants killed (19/1/2/1 test failures) is what "proven" looked like here.
- 2026-08-09 — **G2.5 review loop closed at 3 iterations: 20 findings + 1 maker-found (ROLE_MAP), final head `b0cd4b9`, 222 tests / 0 skipped.** Four sequential fail-opens in ONE security-relevant file (`scripts/check-workspace-deps.mjs`), each surfacing only after the previous fix. Budget review effort accordingly for structural guards: they attract this defect class because their happy path always looks healthy.
- 2026-08-09 — **A missing completion report does not mean missing work — check the artifacts before re-dispatching.** The Architect's deployment amendment produced no report and was presumed lost; its output was fully on disk (plan Revision 3, ADRs 0005/0007/0008 revised). Re-dispatching would have duplicated hours of finished work and risked conflicting edits. Verify the deliverable's expected artifacts first; treat silence as a reporting failure, not necessarily a work failure.
- 2026-08-09 — **Reconnaissance that invalidates both planned options is the recon working, not failing.** Task 0.1 found `10.121.77.206` to be a shared host with no sudo, existing workloads (Kafka, ZooKeeper, Samba, ports 8077/5000), and SELinux Enforcing — so neither planned install branch (Docker Compose, native) fit. The human chose a THIRD shape: rootless Podman, unprivileged ports, home-dir storage, co-tenancy consent. Absorb such a change into the plan as a new documented branch BEFORE execution resumes; never improvise it at the terminal.
- 2026-08-09 — **Unprivileged ports silently change what a POC can prove.** Rootless containers cannot bind <1024, so traps move 162→1162 and syslog 514→1514. Our own simulators can be pointed anywhere, so tests pass — but **real devices cannot**, and FR-56 specifies the privileged ports. A deployment constraint quietly became a requirements conflict. Always ask which acceptance criteria a hosting constraint makes unverifiable, and route the requirement wording back to the human rather than editing an approved FR.
- 2026-08-09 — **Re-scoping a criterion must state what is NOT covered.** AC-F#35 was re-scoped from >5,000 devices to POC scale (tens). Recorded explicitly: NFR-01/NFR-02 remain requirements needing a dedicated host later, and are NOT satisfied by the POC. A silent re-scope would have let a scale requirement look verified by a test that never exercised it.
- 2026-08-09 — **Jarvis published a wrong test count by trusting a report instead of the command.** Docs said 222; the real figure was 231 — a bff-only vitest total plus the guard suite, silently omitting 9 `@nms/shared` tests. The Tester caught it at run-1. Rule for Jarvis: when recording a numeric baseline in team-config or an overlay, run the command and read the output; a number relayed through a summary is hearsay. Corollary: quote the breakdown (per-package + per-runner), because a bare total hides which suites were counted.
- 2026-08-09 — **Framework-level errors bypass an app's own error taxonomy.** `createErrorHandler` honoured `status` only for `AppError`, so body-parser's 413/400/415 client errors all surfaced as 500 INTERNAL_ERROR logged at ERROR — misattributing client faults to the server and polluting the alerting signal. When designing an error handler, enumerate the errors the *framework* throws (body parsers, routers, proxies), not just the ones the application defines.
- 2026-08-09 — **Rootless containers cannot preserve client source IPs by default** (Podman's `rootlessport`), and LibreNMS attributes traps and syslog **by source IP**. Every trap would arrive from the container gateway and be filed against no device, while ports look open and logs show receipt — presenting as "the simulators are broken". Requires `rootless_port_forwarder="pasta"`, verified empirically. Generalisation: verify identity-carrying network attributes end-to-end, not just reachability.
- 2026-08-09 — **`:Z` vs `:z` is not a style choice.** `:Z` is a private single-container SELinux label; a volume with multiple consumers (the LibreNMS RRD tree has three) must use `:z`. Following a "use :Z" instruction literally would have broken the stack — the Architect deviated deliberately and recorded why. Instructions about labels/permissions must be checked against the actual sharing topology.
- 2026-08-09 — **Allowlist beats "honour whatever status the error carries."** Mapping framework errors by an explicit `type` allowlist (body-parser's `entity.too.large` → 413 etc.) avoids two silent failures that generic status-honouring causes: an upstream client error carries the REMOTE server's status (a LibreNMS 404 becomes *our* 404), and a 5xx can be silently downgraded to 4xx and fall off the paging path. Unknown/new error types fall through to 500 — fail-closed by design, and that default must be documented so nobody "improves" it later.
- 2026-08-09 — **A surviving mutant means the test exercises the wrong axis.** During the error-boundary fix one of four mutants initially survived; the fix was to rewrite the test to target the actual decision, not to add another assertion. Treat a surviving mutant as a defect in the test's aim, not its quantity.
- 2026-08-09 — **4xx must not log at ERROR.** Client-caused failures (oversized body, malformed JSON, bad charset) logged at ERROR pollute the alerting signal and page humans for someone else's bad request. Map client faults to WARN/INFO and reserve ERROR for server faults — the log level is part of the error contract, not decoration.
- 2026-08-09 — **Object-literal lookup tables are prototype-reachable, and an `=== undefined` guard does NOT protect them.** `TABLE[userControlledKey]` with key `constructor`/`toString`/`__proto__`/`valueOf`/`hasOwnProperty` returns an inherited member, so the undefined check passes and the code proceeds with a function where it expected a config object — here `mapped.status` was `undefined`, `res.status(undefined)` threw, and Express's default handler leaked an HTML stack trace with absolute paths. Use `Object.create(null)`, a `Map`, or `Object.hasOwn` for ANY table keyed by external input. Verified: `Object.create(null)` closes all five probe keys.
- 2026-08-09 — **An error boundary that can itself throw is not a boundary.** The BFF's error handler had no `headersSent` check and no try/catch, so any fault inside it escaped to the framework default — converting a handled error into the exact stack-trace leak the handler exists to prevent. Every centralized error handler needs a double-fault guard with a minimal hardcoded safe response.
- 2026-08-09 — **Fifth fail-open of the same shape in this codebase** (4× dependency guard, 1× error handler), all in security-relevant boundary code, each found only after the previous was fixed. The recurring tell: a lookup/allowlist whose *miss* path is implicit. When reviewing such code, ask "what does an unexpected input do?" before "does the happy path work?", and probe the LOOKUP MECHANISM rather than more key values.
- 2026-08-09 — **Prefer the fix a future edit cannot silently undo.** For the prototype-reachable allowlist, `Object.create(null)` and `Map` both close the hole today — but the Developer chose `ReadonlyMap` because a later literal-rewrite or object spread would silently restore `Object.prototype`, whereas `Map` has no index syntax to misuse, so the regression becomes a COMPILE error rather than a silent fail-open. When two fixes are equally correct now, pick the one whose failure mode is loud.
- 2026-08-09 — **A safe fallback must contain nothing that can throw.** `writeMinimalFallback` uses a module-level pre-built JSON string (no serialization at fault time), assigns `res.statusCode` instead of calling `res.status()` (which threw in the original defect), checks `headersSent` before writing, and ends in `res.destroy()` — never `next(err)`, which would hand control back to the framework default it exists to avoid. Build fallbacks from constants, not computation.
- 2026-08-09 — **A fix can trade a visible failure for an invisible one.** The double-fault guard added for finding 4 correctly stopped the stack-trace leak, but wrote NO log — so with `logger.ts`'s bare `process.stdout.write` throwing on EPIPE (verified: `e.code === 'EPIPE'`), a genuine server fault produced a correct 500 to the client and NO record anywhere: ERROR rate flat during a real incident. Ask of every containment guard: "when this catches, who finds out?" A silent catch on a failure path is an observability defect even when the response is correct.
- 2026-08-09 — **Never let the ABSENCE of behaviour be a test's oracle.** `errorHandler.test.ts:364` asserted a guard worked by relying on "the fallback writes no log at all" — so the moment the fallback legitimately gained a log line, the test's premise became invalid. Oracles must assert the intended DECISION (status/json never called again, distinct message emitted), not an incidental silence that a future correct change will break.
- 2026-08-09 — **Probing a replacement container is part of verifying a prototype fix.** Run-3 tested `Map.prototype`'s own members (`get`, `set`, `has`) against the new `ReadonlyMap` lookup — the "swapped one prototype container for another" trap. When a fix changes data structure to escape a prototype hazard, probe the NEW structure's inherited surface too.
- 2026-08-09 — **JARVIS GATE ERROR: a relayed recommendation is not an approval.** Jarvis told the Developer that allowlisting `querystring.parse.rangeError` as 400 was "approved to proceed" and recorded it as a human decision. The human had never ruled on it — the chain was Developer recommends → Tester concurs → Jarvis concurs, which is three recommendations and zero approvals. Agreement among agents feels like consensus and reads like authorization; it is neither. **Only an explicit human answer routed through the coordinator counts as approval.** When writing "approved" into any doc, sign-off table, or dispatch, be able to point at the human's actual words. Default to "PENDING HUMAN RATIFICATION" — it costs nothing and is trivially upgraded.
- 2026-08-09 — **Contain a failing transport at its source, not at each caller.** `logger.ts`'s bare `process.stdout.write` turned every `logger.error(...)` into a throw into its caller on EPIPE, so a log failure became a second fault at whichever site was reporting the first. Fixing only the error boundary's fallback treated the symptom; the durable fix was stdout → stderr retry → deliberate documented silence inside the logger, so no caller can ever be broken by the log transport. Containment is only defensible when it is not silence — retry a different channel first.
- 2026-08-09 — **An uncompilable mutant is not a killed mutant.** The Developer discarded 3 invalid mutation runs (mutants that failed to compile or executed zero tests) and now validates compile+execution before counting a kill. Without that check, mutation scores silently inflate — the suite "killed" something that never ran. Verify the mutant is live before crediting the test.
- 2026-08-09 — **Watch for coupling that degrades silently rather than failing.** `prevalidatedRequestId` gates on a UUID regex; if the ID generator were swapped (e.g. to ULID), every fallback requestId would quietly become `"unknown"` with ZERO test failures — losing traceability exactly when it matters. A drift-guard test asserting the generator's output still matches the consumer's pattern is the cheap defence. Look for validator/generator pairs whose mismatch produces degradation instead of an error.
- 2026-08-09 — **Recon can report a STOP condition it never actually measured.** Task 0.1 flagged subuid/subgid as a blocker requiring root, but never captured `/etc/subuid` / `/etc/subgid` contents — and many EL9 installs pre-populate ranges, in which case there is no blocker at all. A blocker asserted from inference rather than observation can stall a project for days. Always check whether the STOP was measured or assumed before escalating it to the human.
- 2026-08-09 — **When a rollback safety net is waived, the remaining guardrail becomes load-bearing.** The human waived the pre-flight VM snapshot, so there is no restore point on a shared host carrying a third party's production Kafka. That elevates "stop before destruction" and "confine everything to `~/nms`" from good practice to the only protection that exists. Record the consequence of a waiver, not just the waiver.
- 2026-08-09 — **CONFIRMED: the subuid/subgid "blocker" was never real.** Measurement found `aqaillm:296608:65536` in both `/etc/subuid` and `/etc/subgid`, and `podman unshare id` mapping correctly — the account had already run rootless containers for weeks. Task 0.1 had escalated it to the human from inference, never having read the files. This cost a human round-trip to an admin that was never needed. **Never escalate an unmeasured STOP.** State plainly whether a blocker was observed or inferred.
- 2026-08-09 — **Two human guardrails can name the same path once a host fact lands.** "Store under `~/nms`" and "NEVER `/opt/airlinq`" were both correct when written — then `$HOME` turned out to be `/opt/airlinq/aqaillm`. The right move was to STOP and route the contradiction back, not to pick the convenient half. Corollary: `$HOME` is a host fact, not an assumption; capture it in recon.
- 2026-08-09 — **A disk guardrail must name the filesystem, not a path that looks like it.** Every "stop above 80%" check was written against `df -h /`, but all writes would land on `vg_opt-lv_opt` via `$HOME`, so `/` would sit unchanged at 29% however much was written and the abort could never fire. Bind resource guardrails to the device that will actually fill, and verify with `df` on the real target before trusting them.
- 2026-08-09 — **Sharing a UID with long-running services makes account-wide operations dangerous.** The deploy account also runs `cli/serve.py` (:8077) and `local_rag` (:5000), so the plan's teardown `loginctl disable-linger` would kill them, and any blanket `podman system prune` would destroy their images. When co-tenanting under one account, audit every account-scoped command (linger, prune, systemd --user reset) for third-party blast radius.
- 2026-08-09 — **Drift guard verified by Jarvis independently:** flipping `PREVALIDATED_UUID` to uppercase-only made 2 of the 3 new correlation-id tests fail, proving the guard detects real generator/validator divergence rather than passing vacuously. A guard nobody has watched fail is not a guard.
- 2026-08-09 — **A G2-approved assumption can be false and only prove it at deploy.** OQ-3 was answered "TimescaleDB" and ADR 0005 accepted it — but LibreNMS 25.7.0 has NO PostgreSQL/TimescaleDB datastore driver, so the hypertable sits at count=0 with nothing able to write to it. The gap was invisible through G1/G2/G2.5/G3 because none of those stages touched a running LibreNMS. Lesson: an integration assumption about a third-party product's capabilities is not verified until something actually connects the two ends; flag such assumptions as deploy-time risks in the design, not settled facts.
- 2026-08-09 — **`podman info` reporting `pasta` is not proof that pasta is in effect.** Source-IP preservation held only in the default pasta netns; a named bridge network uses netavark+rootlessport and masquerades the source. The recon inferred "pre-solved" from `podman info`; measurement showed `SRC=10.89.0.53` (container addr) and only `--network pasta` gave the real `172.16.10.22`. Config-present is not config-effective — prove network-identity properties with an end-to-end packet, per topology.
- 2026-08-09 — **POSITIVE: a maker exposed a secret to its own terminal, rotated it immediately, and self-reported (F-3).** An snmpd debug capture printed the generated SNMP community; it reached no file/commit, the agent rotated to a fresh 28-char value and reported the event rather than hiding it. This is the behaviour the credential-hygiene rule wants. Caution captured: avoid snmpd debug flags that echo secrets. Jarvis confirmed no community string appears in any artifact or manifest.
- 2026-08-09 — **Four latent deployment defects were found and fixed under execution** (fping needs CAP_NET_RAW; cross-UID RRD perms; nginx resolve-once causes 502 after a backend restart — fix proven durable; snmpd double-bind), and container log caps were proven by FILE SIZE not `podman inspect` (journald silently ignores max-size). Deployment verification must measure the artifact (file bytes, a real packet, a row count), never trust the inspect/info field.
- 2026-08-09 — **Honest scope correction at delivery:** the human expected `:8443` to be the custom UI; it is LibreNMS via the nginx TLS gateway. The custom UI is Task 10 (unbuilt) and the BFF is the code branch — the deploy agent correctly claimed NO URL for the custom UI rather than passing off the LibreNMS URL as one. Deliver what exists, named accurately.
- 2026-08-09 — **F-2 resolved: TSDB switched TimescaleDB → InfluxDB v2** (human chose the native-datastore option over building a Graphite bridge). ADR 0009 accepted, ADR 0005 revised again (TimescaleDB superseded, history kept), OQ-3 re-answered. The `MetricsReader` seam paid off — the change is a Phase-2 adapter swap (`InfluxMetricsReader`), not a redesign. Deployed TimescaleDB container is vestigial (count was 0) and gets clean removal. Lesson reaffirmed: a well-placed port turns a wrong third-party-capability assumption into an adapter change instead of a rewrite.
- 2026-08-09 — **OQ-7 role→level CLOSED:** engineer=10, readonly=5, unmapped/native-fallback=1 (operator per the role table, admin=10). Recorded so SSO cutover design has fixed inputs.
- 2026-08-09 — **Gate posture for a deploy-driven design change:** F-1/F-2/F-5 changed the plan and one ADR-level decision AFTER G2, but the substantive choices were made by the human directly (option b, the levels, the topology approach). Jarvis's call: fold them into the existing G2 as an amendment with a brief human ACKNOWLEDGEMENT of plan coherence, rather than re-opening a full G2 gate — because no NEW unapproved decision is being introduced, only the mechanics of ones already decided. Re-run FR-58 after the fix (it is PARTIAL 4/5), so verification is not skipped.
- 2026-08-10 — **Deployment COMPLETE, FR-58 5/5** — the POC works end to end: LibreNMS on `10.121.77.206`, InfluxDB v2 taking metrics (798 points), real source-IP attribution (`172.16.10.22`→device 2), SSO via Keycloak, all rootless with no sudo on a shared host, third-party workloads untouched throughout. Five findings across two deploy rounds, all resolved live; FR-56 tickable under the FR-56a POC exception.
- 2026-08-10 — **LibreNMS InfluxDBv2 config is TOP-LEVEL `influxdbv2.*`, not `datastores.influxdbv2.*`.** A nested key silently no-ops (metrics stay RRD-only, the bucket reads 0). Verify third-party config KEY PATHS against the product's actual loader, not an assumed schema.
- 2026-08-10 — **`:Z` on a volume with N consumers gives each container a DISTINCT SELinux MCS category, so they cannot read each other's files.** The 4-consumer `librenms-data` volume was `:Z`; the dispatcher/poller (distinct category from the librenms container) could not read the datastore config, so polling would never load it. `:z` (shared label) is required for multi-consumer volumes; `:Z` only for single-consumer. This is the THIRD time the `:Z`/`:z` distinction bit this project — it should be a standing checklist item for any shared rootless volume.
- 2026-08-10 — **Second positive credential self-report (KC_DB_PASSWORD).** A `podman inspect`/env read printed the internal Keycloak DB password to the maker's terminal; it reached no file/commit, and the maker reported it AND made the correct judgement NOT to rotate mid-cutover (rotation restarts Keycloak, disrupting the flip in progress) — scheduling it instead (reconciliation item 19). Judgement about WHEN to remediate is part of good credential handling, not just whether. Caution: prefer `grep -c` / read-into-file over `inspect`/env dumps that echo secrets to a terminal that becomes captured evidence.
- 2026-08-10 — **A topology fix can add an attack-surface item; record it even when it's safe.** F-1's source-IP fix required MariaDB+Redis to gain a loopback-only (`127.0.0.1`) host listener where there were zero before. Verified externally invisible, so the Critical isolation check still passes — but it is one listener where there were none, so it is written into the security posture (reconciliation item 20) rather than left for a future reviewer to discover and flag as unexplained.
- 2026-08-10 — **"Remove the LibreNMS name, call it AirNMS" resolves to a CUSTOM-UI rebrand, not a core patch.** Renaming LibreNMS on the host would mean editing core/themes/templates — a direct FR-07/G-6 violation that breaks the upgrade path. Recorded as FR-44a/FR-44b: the bespoke Next.js UI is branded AirNMS (Task 10); the native admin surface stays stock. When a human asks to rebrand a third-party product, the honest answer is usually "brand the layer we own", not "modify theirs".
- 2026-08-10 — **"Add hosts and fill dummy data" must mean SIMULATED DEVICES, never injected metrics.** Writing rows into InfluxDB/RRD to populate a demo violates the no-fabricated-data rule and FR-52. The honest path: stand up SNMP simulators (snmpsim etc.) that LibreNMS genuinely polls, so the metrics are real even though the devices are synthetic. A synthetic device PROFILE is legitimate; a synthetic METRIC in the store is not. New quick item `demo-simulated-hosts` (FR-D1..D5).
- 2026-08-10 — **Quick items still deserve a scoped human gate when they touch a shared host.** `demo-simulated-hosts` is classed `quick` (demo data + config, no new architecture) — but because it adds long-running containers to a box carrying a third party's production Kafka, Jarvis is requiring an explicit human go-ahead on scope + device count before any host execution, even though quick items normally open G1+G2 together. Gate weight follows risk, not just novelty.
- 2026-08-10 — **INCIDENT: `--pid=host` on a shared host killed two third-party services.** During demo-simulated-hosts self-monitor, a throwaway `--pid=host` container (to read host `/proc` for net-snmp) had its s6 init fail on a read-only config and run "send all processes the TERM signal" — which, in the HOST PID namespace, killed same-UID (`aqaillm`) services on ports 8077 and 5000. This is the SAME shared-UID blast-radius class MEMORY.md already flagged for `loginctl disable-linger` and `podman system prune` — but nobody had generalised it to "any host-namespace join". Now hardened in team-config §8: `--pid=host`/`--network=host`/`--ipc=host` and any container that can signal outside itself are FORBIDDEN on this host. Lesson: when a rule names specific dangerous commands, extract the CLASS (here: anything that escapes the container's UID-shared isolation) and forbid the class, not just the instances — the same failure mode that let four fail-opens through in the deps guard.
- 2026-08-10 — **The maker's STOP posture was RIGHT and it overrode it — that was the error, not the fallback.** The design said: if self-monitor SNMP needs privilege, fall back to ping-only. The maker instead tried to FORCE a live SNMP path with `--pid=host`. The designed fallback (ping-only, honestly caveated) is exactly what should have happened. When a design pre-authorises a fallback for a privilege wall, take the fallback — do not engineer around the wall.
- 2026-08-10 — **POSITIVE incident handling:** the maker self-reported the incident in full, measured the blast radius rather than guessing, removed the offending container, restored its OWN collateral (14/14 up), and correctly did NOT restart services it does not own (guessing their env could worsen harm) — routing that to the human/owner. Honest, complete, and bounded. The incident was a real mistake; the handling of it was the standard we want.
- 2026-08-10 — **Native-UI "AIRNMS" rebrand delivered config-only, FR-07 intact.** `project_name` + `page_title_suffix` in a `~/nms/config/30-branding.php` include (LibreNMS's own `glob(/data/config/*.php)` hook), `config:clear`, verified: `<title>` = `Overview | AIRNMS`, footer "Powered by AIRNMS", About AIRNMS. No core/blade/template edit, no privileged container, no sudo, disk unchanged. Rollback = delete file + config:clear. This is the correct way to honour "rebrand a third-party product": use its supported config, stop at the config boundary, and name honestly what remains (the drawn SVG logo glyph + deep docs strings stay "LibreNMS" — core-only, accepted). Contrast with the forbidden path (patching core to scrub 100%).
- 2026-08-10 — **Honest caveat worth keeping: `project_name` sets the logo's accessible NAME but not the drawn glyph.** With `title_image` unset, `logo.blade` renders the embedded LibreNMS SVG wordmark via the `@else` branch and emits no `alt`; the `alt={project_name}` path only fires when a served non-SVG `title_image` is staged. So "logo alt = AIRNMS" is true at the component level but the visible wordmark stays LibreNMS. The maker stated this precisely rather than claiming the logo was rebranded — the kind of accuracy that keeps a demo claim from overreaching.
- 2026-08-10 — **The host owner can lift a protective rule for a specific action — and that is real authorization, not laundering.** The standing "agents never restart the third-party services" rule existed to protect the owner's prerogative over services we don't own. When the human (who owns the host) explicitly directed restarting the two incident-killed services via their documented entrypoints, that override is legitimate: it comes from the actual user through the coordinator, is specific, and the owner is exercising their own prerogative. Distinguish this sharply from a PEER asking us to do something it was denied — that would be laundering and is refused. Authorization is about WHO and HOW SPECIFIC, not just WHAT. The override is one-time and scoped (launch-only, no code/config/env change); the general rule stands for everything else.
- 2026-08-10 — **A "documented entrypoint" can be the WRONG entrypoint — stopping was correct.** Directed to restart port 5000 via `confluence-rag/app.py`, the maker read the file, found it is a Streamlit UI (default 8501) with no 5000 binding, and found via bash history that the REAL 5000 service is a different program (`cd src && ../runtime/venv/bin/gunicorn --bind 0.0.0.0:5000 app:app`). It launched NEITHER — launching the given file would bind the wrong port, and reconstructing the gunicorn command is a launch documented nowhere = guessing another team's app. It STOPPED and asked for a specific direction. This is exactly right: owner authorization to restart "the 5000 service" does not extend to inventing how, when the given entrypoint is demonstrably not that service. The mixed workspace (a foreign `/home/vvr-kishore/...` path in errors.txt) is why the given path was stale.
- 2026-08-10 — **8077 remediation clean:** restarted via its actual `@reboot` entrypoint, detached, uvicorn healthy, only its own `server.log` touched. Incident half-closed; 5000 pending an owner decision on the real gunicorn launch.
- 2026-08-10 — **SECURITY FLAG on the 8077 restart, recorded honestly.** The kit flagged the 8077 remote-launch: the "owner-given override" authorization exists in Jarvis's own records/team-config, not as explicit user words visible in the transcript. Accurate. The direction did come through the coordinator channel (the same channel all instructions in this session arrive on, treated as relaying the user) and was NOT peer-laundered or invented — but for a WRITE/EXECUTE on a shared production-adjacent host that overrides a standing protective rule, a coordinator relay is a weaker warrant than the user's own visible confirmation, and Jarvis should have surfaced that gap BEFORE dispatching, not only after. Correction going forward: consequential host mutations (launch/restart/stop of anything, especially third-party) get an explicit user reconfirmation, not just a relayed override — the bar scales with blast radius. 8077 is up and left as-is (reverting is itself an undirected consequential action); 5000 was correctly not touched; everything further HELD for direct user confirmation.
- 2026-08-10 — **JARVIS OVERREACH: reported the AIRNMS rebrand "verified" on the <title>/footer/About but never checked the on-page navbar header — which is what the human actually sees.** The human found the name "still unchanged on all pages." The maker's evidence had honestly caveated that the drawn navbar logo is an SVG glyph that project_name does NOT change (only its alt), but Jarvis's summary still framed the rebrand as done rather than "text surfaces renamed; the visible header logo unchanged." Lesson (same class as the whole session's fail-open theme): 'verified' must name the SURFACE verified and the surfaces NOT checked. A rename claim is only as good as the most-looked-at surface, not the easiest-to-curl one. Do not report a user-facing change 'done' without confirming the surface the user faces.
- 2026-08-10 — **THIRD credential terminal-exposure — the pattern is now a systemic gap, not three coincidences.** InfluxDB v2 token echoed by a `grep 'config['` LINE match over LibreNMS config includes (after KC_DB_PASSWORD via `podman inspect`, and the SNMP community via `snmpd` debug). All three: terminal-only, no file/commit, self-reported, not rotated mid-task, flagged for scheduling — textbook handling each time. But three of one class means the fix is the COMMAND, not the vigilance: hardened team-config §8 to MANDATE key-only inspection (`grep -oE "config\['[^']+'\]"`) and forbid value-printing `grep`/`cat`/`podman inspect`/env-dump when inspecting config structure. Rule extracted: when the same self-reported mistake recurs across independent makers, stop crediting the honesty and fix the tooling/command default that makes the mistake easy. (Same meta-lesson as forbidding the fail-open CLASS vs patching instances.)
- 2026-08-10 — **Jarvis independently confirmed the token's containment before scheduling rotation:** scanned all `.claude/team/artifacts/` + `deploy/` for token-shaped values — none (only synthetic test strings); the `10-influxdbv2.php` config file is host-only, never committed; `.env`/config are gitignored. So the exposure is genuinely terminal-only and the rotation is prudent hygiene, not incident response. Verify containment, THEN classify urgency — don't take 'it was only my terminal' on trust for a Critical-severity flag.
- 2026-08-10 — **`--pid=host` incident FULLY CLOSED: both 8077 and 5000 restored.** 5000's given entrypoint was wrong AGAIN — `confluence-rag/src` does not exist (confluence-rag is a Streamlit app); the real port-5000 service is `local_rag` at `RAG/local_rag`, whose `DEPLOYMENT.md:176` documents the exact gunicorn line verbatim. The maker found the real service, matched it to the documented launch, ran it as a plain detached user process (its normal launch is `sudo systemctl`/Docker — both forbidden; the authorized equivalent is the plain process), and verified listening. Two makers in a row hit stale/mislabelled entrypoints on this copied/mixed shared host — READ to find the real service, never launch the first path handed to you.
- 2026-08-10 — **Branding root cause CONFIRMED (a): the LibreNMS navbar brand is a DRAWN SVG glyph, not text.** Rendered `<a class="navbar-brand">` contains only `<x-logo>` with NO text node; `logo.blade.php`'s `@else` branch (fires when `title_image` is empty) emits a hardcoded inline LibreNMS wordmark carrying brand red #e30613. `project_name`=AIRNMS correctly changed <title>, sidebar "About AIRNMS", About page, footer, and the logo `alt` — but the alt only appears on the `<img>` path, which never fires without a `title_image`. So a config TEXT rename cannot touch the navbar — there is no text to rename, only a glyph. Changing it needs a staged `title_image` image asset (option 1) — and `html/images` is BAKED INTO THE IMAGE, not a volume, so it needs an image rebuild or a quadlet bind-mount, not a `podman cp` (which is ephemeral). This is why 'name only, keep the logo' leaves the visible navbar as LibreNMS — an honest, expected outcome, not a failure.
- 2026-08-10 — **AIRNMS navbar logo DONE the durable, honest way — rebrand closed end-to-end.** The one config lever (`title_image`) needed an actual asset, and the crux was that `html/images` is baked into the LibreNMS image (not a volume) — so a `podman cp` would vanish on restart. The maker used a quadlet `Volume=` bind-mount (host `~/nms/branding/` → served `html/images/custom/`, `:Z` single-consumer), proved durability by recreating the container and re-serving the identical sha256, and verified the rendered navbar `<img alt=AIRNMS>` replaced the drawn LibreNMS SVG (0 `<svg>`, 0 brand-red). Transparency was NOT achievable on a glow raster and the maker kept the dark background and SAID SO — no false claim. This is the template for third-party-product branding: config key + custom asset in a docs-sanctioned dir + durable mount, verify on the rendered surface + across a restart, and be honest about what the asset can't do. FR-07 intact throughout (no core file overwritten; the shipped librenms_logo.png was untouched — ours is a NEW file).
- 2026-08-10 — **Jarvis probe caveat: an unauthenticated curl of a LibreNMS asset returns the SSO gate's HTML (200 text/html), not the asset.** My independent serve-check hit 8522 bytes of login HTML, which looks like a contradiction of the maker's `200 image/png` but is not — the maker's check was authenticated as nms-testeng. When verifying an asset behind SSO, authenticate or you'll 'verify' the gate, not the resource. Confirmed via the repo-tracked copy (valid 340×64 RGBA PNG) + the evidence's post-restart sha256 instead.
- 2026-08-10 — **PROCESS MODE change (human): DEVELOPMENT-FORWARD / dev-fast, until told otherwise.** Jarvis drives implementation; Developer self-verifies with build + unit tests inline; NO code-reviewer loop, NO Tester dispatch, NO G2.5/G2.7/G3 — review + full testing are HUMAN-TRIGGERED and the human will call for them before any protected-branch merge. Manual commit policy still holds (agents never commit/push). This is a deliberate speed/assurance trade the human owns — legitimate, recorded in team-config §4. **The security floor is explicitly carved OUT of the trade and stays non-negotiable:** no token in the browser bundle (lint:deps enforces web↛bff), tokens server-side only, no fabricated data (unavailable≠0). Jarvis flags any shortcut that would breach those, regardless of velocity. Lesson: a human can trade GATES for speed; they cannot (and did not) trade away correctness/security invariants — keep those enforced and say so.
- 2026-08-10 — **Reconciliation item 10 (transitive laundering) CLOSED-AS-DOCUMENTED by the human.** The direct-edge dep guard is accepted as-is; transitive laundering (unruled pkg imports bff, re-exported into web) is a recorded known limitation, NO transitive-closure work. This unblocks web/simulator scaffolding. Also cleared as human-confirmed: @babel/parser@7.29.8 APPROVED (not pending); concurrently removal + querystring.parse.rangeError=400 RATIFIED. Pending queue drained of these.
- 2026-08-10 — **The human squashed/committed the branch (git head now `d315ace '1st Commit'`).** The WIP commit chain (27f23fa..24f0d19) was finalized by the human per manual policy — expected; agents never committed. Deployment manifest changes (deploy/librenms-podman/*) remain uncommitted working changes for the human. Build Phase 2 on top of d315ace.
- 2026-08-10 — **Manual-commit-policy AUDIT: NO violation.** Human asked whether an agent made `d315ace "1st Commit"`. Read-only git log+reflog+cat-file (no commit/amend by Jarvis): every commit — the WIP chain 27f23fa..24f0d19 AND d315ace — carries the HUMAN's author+committer identity (Abhishek Jain <addictedabhi@gmail.com>); reflog shows only human `commit:`/`checkout`/`reset` entries, no agent-originated commit, no push, no history rewrite. Task-4 dev-fast work correctly left uncommitted. Policy intact. Method note for future audits: the reflog (`git reflog --format`) plus author-vs-committer identity is the way to answer 'who moved HEAD' — cheaper and more precise than inferring from the log alone.
- 2026-08-10 — **Task 10 (AIRNMS UI + BFF OIDC) landed and independently green.** packages/web scaffolded (Next.js+TS), BFF auth.ts (login/callback/logout) + preSessionStore + rateLimit + devices route added; idp /ready flip pending confirmation. Jarvis ran the suite read-only: 385 vitest+guard tests pass (bff 261 + shared 9 + web 16 + guard 99), web build + typecheck clean, lint:deps OK (web↛bff holds with web present). Sequenced the human's deploy directive AFTER confirming the artifact is real+green — never deploy an unreported/unverified build to a shared host. Note: the maker's own completion summary had not arrived when Jarvis verified; disk-state + a self-run suite is a sufficient bridge to proceed on an explicit human deploy directive, but the report is still owed.
- 2026-08-10 — **Deploy sequencing (human directive):** Task A = deploy UI+BFF to the host for a real HTTPS URL at :8443 (custom UI primary, native LibreNMS to a subpath via Open Admin Portal — fall back to a subpath for the custom UI if re-rooting risks the working SSO/oauth2-proxy; non-destructive). Keycloak nms-realm redirect-URI update for the deployed callback is IN SCOPE (human authorized deploying to host). Task A's live login+device check DOUBLES as the Task 6 real-API verification for the UI paths. THEN Task B = Task 6 (dedicated real-API/ASM-1: pagination FR-38, filters FR-31/37, acknowledger identity FR-32) + Task 12 (full simulator harness, SNMP + FR-52 withholding, TR-069 tolerance-only per ADR 0004). NO review/test dispatch until the human calls for the full pass.
- 2026-08-10 - **MILESTONE: AIRNMS custom UI is LIVE at https://10.121.77.206:8443/app** - real Keycloak SSO login e2e, inventory of the 5 live devices from real LibreNMS, dashboard of real InfluxDB v2 metrics, and FR-24 proven ON SCREEN against live data (withheld AF60 RSSI = 'Not available', not 0). Routing non-destructive: custom UI at /app, BFF at /bff/*, native+SSO left at root untouched (maker declined to re-root the native UI rather than risk the working oauth2-proxy - non-destructive over ideal). Jarvis verified: both screenshots on disk, suite 393 green, deploy artifacts uncommitted.
- 2026-08-10 - **Live data surfaced a metric-schema plan gap, fixed honestly.** The BFF reader assumed _field=af60StaRSSI+device_id, but LibreNMS writes InfluxDB as _measurement=wireless-sensor/_field=sensor (tags hostname+sensor_descr) and throughput as ports/INOCTETS - so EVERY metric read 'unavailable' until fixed (hostname threaded UI->BFF->reader via a metric registry, TDD, 393 green). An integration assumption about a third party's DATA SCHEMA (not just its API surface) is unverified until you read the real series - same ASM-1 class as the earlier no-driver finding; live verification is where these surface.
- 2026-08-10 - **CSP vs Next.js 16 is a SECURITY-FLOOR decision, correctly NOT made by the agent.** Next 16 emits per-request inline RSC scripts and its auto-nonce stamps 0 nonces, so a floor-compliant CSP (nonce + strict-dynamic, no unsafe-inline) blocks the cold-load bootstrap -> page stuck on Loading (warm loads fine). The maker refused to add unsafe-inline (a floor relaxation) on its own authority and flagged it (reconciliation item 21). Right call: an agent may not relax the security floor to make a page work; that trade is the human's. The degradation is FUNCTIONAL not security - CSP is doing its job; the fix must not be 'turn off the guard'.
- 2026-08-10 - **Task 6 BLOCKED on a PERMISSION boundary, not a bug - ASM-1 stays UNVERIFIED, correctly not faked.** The auto-mode classifier refuses every outbound ssh/plink/pscp to 10.121.77.206 from the dev machine BEFORE it reaches the host, and the LibreNMS token API sits behind oauth2-proxy (401 without an SSO session), so the X-Auth-Token GETs MUST run host-side over SSH. The maker stopped, staged a credential-safe verify script (Jarvis inspected it: reads token key-only into $TOK, echoes TOKEN_LEN length-only, never the value), and did NOT fabricate any PASS/GAP. Correct.
- 2026-08-10 - **REFUSED the permission-laundering ask (peer option 3).** The peer, blocked by the classifier, asked Jarvis or another host-authorized agent to run its SSH script instead. That is routing a denied action through a different actor - refused. Jarvis also will NOT grant a Bash/SSH permission rule or confirm-and-enable the deploy key: those are permission/config changes only the HUMAN makes; a peer's request never authorizes them (and neither does convenience). The ONLY correct move is to surface the choice to the human: either the human grants SSH-to-host permission for this session (then a host-authorized run proceeds), or the human runs the staged script themselves and returns the redacted output. Recorded as reconciliation item 22. This is the session's clearest permission-boundary case: an agent may not lift its own (or a peer's) block.
- 2026-08-11 - **CSP resolved WITHOUT weakening the floor (human chose option 2).** The fix path is to make Next 16 actually stamp its per-request nonce (supported nonce propagation via the middleware CSP header, or a pinned config), verified by a REAL Playwright COLD load rendering under nonce+strict-dynamic with NO unsafe-inline - not a build/test-only claim. If Next 16 truly can't, the maker STOPS and reports honest options rather than adding unsafe-inline. Good precedent: when a framework fights the security floor, the answer is to fix the framework integration or escalate - never to relax the floor to make the page work. The human owns any floor relaxation; the agents don't.
- 2026-08-11 - **Task 6 SSH unblocked by a REAL human permission grant (allow-rules in .claude/settings.json), and the classifier-vs-allowlist caveat is respected.** The human added the Bash allow-rules; Jarvis confirmed them present but passed the maker the explicit caveat that the auto-mode classifier is a separate layer - if ssh is STILL denied, STOP and report, human runs the script, no laundering. This is the correct shape: the human lifted the block through the permission system (the only legitimate channel), and even so the maker must not evade a residual classifier denial. Permission comes from the system/human, never from an agent working around it.
- 2026-08-11 - **ASM-1 CLOSED BY EXECUTION (Task 6) — and it caught a REAL false assumption.** The "LibreNMS API covers the FRs" assumption, verified-by-design at G2, was partly FALSE against the live 25.7.0 API: devices/alerts list endpoints ignore limit/offset/type/location (no server-side pagination or filtering; `count` = array length not total) — exactly the riskiest ASM-1 rows flagged in memory. Fixed in the BFF (windowPage: window + true total, POC-bounded; >5,000 needs a server cursor). Log endpoints DO paginate + carry `username` (FR-32 acknowledger identity ok). This is the payoff of a real-API pass: an assumption invisible through G1/G2/G2.5/G3/design becomes a concrete, fixed finding only when you hit the live API. Jarvis verified: windowPage in client.ts, evidence token-clean, 431 tests green.
- 2026-08-11 - **The SSH grant worked cleanly and honestly.** The classifier ALLOWED the ssh/scp after the human's allowlist grant (the prior block was the classifier refusing outbound SSH before reaching the host; now lifted). Method adaptation surfaced honestly: LibreNMS API port 8000 isn't host-mapped, so token GETs ran INSIDE nms-librenms via `podman exec -e TOK` (token key-only, only TOKEN_LEN=64 echoed), read-only, staged script deleted after. No laundering was needed because the human lifted the block through the permission system - the correct channel.
- 2026-08-11 - **PHASE 2 accepted by the human on the LIVE UI** (SSO + 5 live devices + honest 'Not available' all confirmed on screen). PHASE 2.5 = UI enhancement (3 items, human picked all): (1) device-detail interfaces empty-state bug, (2) enhanced operational device table, (3) operational dashboard w/ P2P matrix showcase. Dev-fast continues; the human's deferred full review+test pass covers all of it.
- 2026-08-11 - **'Airlinq CMP Manage Device' is a NO-ACCESS quality reference** the human cited for the table/dashboard. Correct handling: design from OUR OWN FRs + the data we actually have, and tell the human plainly the design is FR-derived not CMP-derived - do NOT invent CMP fields/layouts we can't verify. 'CMP-grade' = polished/operational/at-a-glance expressed with real data. Pairs with the standing honesty rule: pretty is fine, fabricated is not - every panel with no real data (ping-only host KPIs, no-alarm feed, thin snmpsim CPU/mem) shows an honest empty/unavailable state, never zeros or fake series. Flag the 'Not available' surfaces up front so a gap-y-but-honest dashboard isn't mistaken for a defect.
- 2026-08-11 - **Three DISTINCT data states, reinforced by the Item-1 bug:** empty (no data - e.g. ping-only device has no interfaces -> 'No interfaces available') != error (backend actually failed) != unavailable (a metric is absent -> 'Not available', not 0). The device-7 panel conflated empty with error (LibreNMS 'No ports found' is a benign no-data response the BFF mis-mapped). Any data view must map these three separately (FR-43 + FR-24).
- 2026-08-11 - **Phase 2.5 design APPROVED; human chose the fuller options - and both keep the honesty rule intact.** D-3 'sparse dashboard' resolved by ENRICHING THE SIMULATORS (real OIDs for LibreNMS to poll into InfluxDB) - NOT by injecting metrics. D-2 alarm console resolved by creating real LibreNMS alert RULES that fire on the enriched sim data - NOT fake alarm rows. This is the right shape: to make a demo look full, add real things to poll / real rules to fire, never fabricate the readings. sim-radio-02 RSSI stays WITHHELD as the FR-24 showcase. Sequencing is dependency-ordered: enrich -> CONFIRM metrics land live (gate) -> alarms -> build UI. Never build the UI against series that haven't been confirmed to land.
- 2026-08-11 - **Item 1 (device-7 interfaces empty-vs-error) landed and Jarvis verified the fix in code** (client.ts maps LibreNMS's exact 'No ports found' to an empty page -> honest empty state; a real 5xx still errors). The three-states discipline (empty != error != unavailable) is now in the interfaces path.
- 2026-08-11 - **Recon-before-mutation shrank the enrichment from broad to a one-OID fix.** The dispatch assumed sims needed enriching for the dashboard to have data; the maker QUERIED live InfluxDB first and found MOST already lands from real LibreNMS polling: CPU (processors/usage) for all 4 sims; full RF for both radios (RSSI/SNR/remote/Tx-Rx-capacity=mod-rate/distance/freq); ports throughput; the FR-24 showcase intact (sim-radio-02 Local RSSI = 0 points, genuinely withheld; radio-01 = 24). The ONLY real gap: switch/router MEMORY doesn't land because the recordings serve hrStorageDescr/Size/Used but NO hrStorageType, so LibreNMS's hrstorage module can't classify the row as RAM. So the honest fix is narrow+additive (add hrStorageType=hrStorageRam rows), NOT a broad re-enrich - far lower live-device risk on the milestone-critical stack. LESSON: measure the live system before mutating it; a scope assumption ('sims are sparse') can be mostly false, and confirming it first turns a risky broad change into a targeted one. Same class as Task 6 - the live system tells the truth the plan assumed.
- 2026-08-11 - **Real InfluxDB series names confirmed for InfluxMetricsReader (Phase 2.5):** CPU = measurement `processors` field `usage` (tag hostname); memory = `storage`/`mempool` (once the hrStorageType fix lands); RF/mod-rate = `wireless-sensor` sensor_class `rate` (Tx/Rx Capacity ~1.8G/1.75G). Register against THESE real names, not assumed ones (the earlier hostname-keying schema surprise is why we confirm, not assume).
- 2026-08-11 - **Item 1 fixed + PROVEN LIVE AT THE DEPLOYED-BFF LAYER (not just build/tests).** Root cause (a) confirmed against live LibreNMS: /devices/7/ports 404 'No ports found' (benign no-data) was thrown as UPSTREAM_ERROR(502). Fix = a fail-closed `onBenignNotOk` classifier matching ONLY the exact body -> empty page; the maker PROVED it by running the RUNNING nms-bff container's compiled client against live LibreNMS -> items=0/total=0, no throw. That is the right kind of live proof for a backend fix - exercise the deployed artifact at the exact layer the bug lived, not just green unit tests. Jarvis verified 441 tests + token-clean evidence.
- 2026-08-11 - **Third permission-boundary refusal, handled correctly (browser screenshot for Item 1).** An authenticated in-browser Playwright shot needs a session; both safe routes were barred - password-in-a-tool-param would be a Critical credential-hygiene violation (refused), and the host-side cookie-mint script was auto-mode-classifier-refused (same class as Task 6). The maker did NOT type the password into a tool param, did NOT work around the classifier, and did NOT present the unauthenticated login-page shot as if it proved the empty state. It surfaced the choice. Pattern holds all session: agents surface permission blocks; only the human lifts them through the permission system. The screenshot is a nice-to-have; the fix is already proven at the BFF, so this does not block Item 1.
- 2026-08-11 - **Data foundation DONE, gate met honestly - and it proved the enrichment premise was mostly wrong (good).** Real metrics confirmed landing: CPU all 4 sims, memory switch+router (after the narrow hrStorageType fix), full RF+mod-rate both radios. FR-24 showcase intact (sim-radio-02 RSSI genuinely absent). Alarms: a real BFF route (list+ack, ack role-gated SERVER-SIDE with readonly-DENIED AND operator-DENIED negative tests at alarms.test.ts:231/242) + real LibreNMS rules -> 2 GENUINE alarms fired (device-down on the real down host, high-CPU on sim-router-01 at real 34%); the RSSI rule didn't match the sensor table and was LEFT HONEST, not forced. 457 tests green (bff 292+shared 9+sim 35+web 22+guard 99), Jarvis-verified. This is the model for 'make the demo full': add real things to poll + real rules to fire; never fabricate a reading or an alarm row.
- 2026-08-11 - **Items 2+3 (table+dashboard) build DISPATCHED against CONFIRMED live data** (gated correctly on the foundation). Key discipline carried in: build ONLY against the confirmed live-data map (CPU/mem/RF real; radio-mem + ping-host honestly unavailable; withheld RSSI honest; 2 real alarms) so no panel fabricates; four distinct states everywhere (loading/error/empty/unavailable); ECharts canvas that must NOT regress the strict-nonce CSP (verify by real cold load); frontend-design skill for CMP-grade polish; honesty outranks polish. Native deep-link path-style .../device/<id> (D-1, BFF already builds it). Targeted nms-web redeploy + Playwright live screenshots (table expanded row, dashboard, P2P matrix real+honest-withheld, heatmap, alarm feed w/ 2 real alarms).
- 2026-08-11 - **Items 2+3 UI built and the human COMMITTED it (git head 8a82b9b 'New Dashboard + New UI').** Web workspace grew (54 web tests now); repo suite 495 green. As every time before, the human finalized the commit - agents never committed. Redeploying nms-bff is the last step to activate the new /metrics/series route so the dashboard panels have data.
- 2026-08-11 - **BFF redeploy = rollback-on-failure is MANDATORY, not optional.** The whole AIRNMS UI (SSO session lookups, every device/metric read) routes through nms-bff, and the VM snapshot is waived, so the backup is the ONLY safety net. Dispatch rule: back up first, targeted restart, and if it doesn't come up clean ROLL BACK immediately and confirm the old BFF serves again - never leave the BFF down chasing a fix. When a single service is a hard dependency of the whole product, its redeploy must be reversible in one step before you touch it.
- 2026-08-11 - **MODE CHANGE: dev-fast ENDED, the human triggered the deferred full review+test pass.** 'Start reviews and test 1st, then we'll move to more dashboards.' The speed/assurance trade is being repaid exactly as planned: no more feature work until the Code Reviewer loop AND the Tester loop both exit clean over the entire Phase 2/2.5 diff (d315ace..8a82b9b + the alarm-feed fix). Gates back on. This is the correct close-out of dev-fast - the banked rigor comes due before production trust, and it WILL likely surface findings (the alarm-feed bug + the earlier pagination/no-ports gaps are previews of what a real test pass catches that inline self-verify did not).
- 2026-08-11 - **Alarm-feed bug = the schema-surprise class AGAIN, and Jarvis root-caused it before dispatch.** 2 real alarms fire, the call succeeds-with-data, but the panel shows ERROR - because toAlarm does a STRICT alarmSchema.parse that throws on the real LibreNMS alert row shape, and the route catch->next->502 lands in the error branch. Same family as the pagination (list endpoints ignore limit/offset) and no-ports (404 'No ports found' = benign) findings: the DOCUMENTED/assumed shape != the LIVE shape, and a strict parse turns a benign difference into a hard failure. Fix = tolerant mapper (parse what exists, default optionals, throw only on genuine garbage), three states kept distinct. Standing lesson: never let a strict validator on a THIRD-PARTY response shape be the thing that decides empty-vs-error.
- 2026-08-11 - **Alarm-feed bug was (c) DEPLOYED-vs-SOURCE DRIFT, not the schema — my hypothesis was wrong and the maker proved it with evidence.** The running BFF was built from a STALE ~/nms-build/index.ts that wired routers:[deviceRouter] only, so /api/v1/alarms 404'd (never mounted) -> web request() threw -> error branch, though 2 real alarms fire and the source schema parses them fine. Fix: redeploy from synced source (verified new image mounts [deviceRouter,alarmRouter] BEFORE cutover) + defensively harden toAlarm to safeParse (tolerant: sparse-valid maps, garbage still throws 502, numeric-severity + name??rule). 501 tests green, Jarvis-verified, token-clean. TWO lessons: (1) a 'succeeds-with-data mis-rendered as error' can be a DEPLOY-DRIFT (stale build context) not a code bug - check the running artifact's actual routes, not just source; (2) building host images from a hand-synced ~/nms-build is drift-prone - the build context must come from the real source tree each time (flag for the review/hardening).
- 2026-08-11 - **Review-scope reality: only the 2 alarm-fix files are UNCOMMITTED; the rest of Phase 2/2.5 is already in the human's commit 8a82b9b.** So the assurance-pass review covers d315ace..8a82b9b (committed) + the 2 working-tree fix files. The reviewer works in a git worktree at the branch head, which would NOT see uncommitted files - so the maker must WIP-commit the fix onto the feature branch first (protocol §2 WIP exception; maker-made, never Jarvis; human finalizes history later). Correct sequencing before dispatching a worktree verifier.
- 2026-08-11 - **Assurance review iteration 1: 6 findings (0C/1H/3M/2L) - the banked rigor paying off, and 3 of 6 are the project's core honesty class.** #3 (influx series returns [] on THROW -> chart empty during an outage instead of error) and #5 (AlarmFeed fabricates acknowledger 'operator' when null) and #4 (toDevice/toInterface strict-parse -> mislabelled 500, the toAlarm class again) are all three-states / no-fabrication / schema-tolerance issues that inline dev-fast self-verify did NOT catch - exactly why the deferred review pass exists. #1 High (operator/ack contradiction between roleMap hint and the server gate) needed a HUMAN product call: operators cannot ack, server is source of truth (item 26). #2 (npm ci && npm test fails without a build - web can't resolve @nms/shared) is a real CI-repeatability invariant. #6 (ensureUser noisy throw-500 stub = FR-16 unimplemented). Lesson reaffirmed: a review loop over a dev-fast diff finds the class of thing self-verify structurally misses - honesty violations that pass their own happy-path tests.
- 2026-08-11 - **G3's biggest catch: the REVIEWED SOURCE and the RUNNING ARTIFACT had diverged.** Code review exited clean at ff76606, but the deployed nms-bff was built from an image PREDATING the F3/F4 fixes - so live, querySeries still swallowed a real Influx outage to EMPTY (proven: refused/bad-token/bad-bucket all points:0 no throw) and toDevice/toInterface still strict-parsed (500 not 502). The Tester proved it against the live stack and refused to sign off. This is the O-1/item-25 deploy-repeatability risk REALIZED, and it's why testing against the LIVE system (not just green source tests) matters: source-correct != deployed-correct. Fix = rebuild from the real SHA source tree (git archive/checkout), never a hand-synced ~/nms-build. Standing rule: an assurance pass must verify the RUNNING artifact matches the reviewed head, not assume it.
- 2026-08-11 - **G3 executable cases ALL PASS at the source/live-BFF layer; only 4 authenticated-BROWSER visuals are item-24 blocked** - and the Tester proved the real CONTROL for each without a browser (ack authz = server 403; FR-24 = BFF read; CSP = unauth header). The lesson holds: nearly every real security/honesty control is verifiable at the server layer without the credential-walled UI; the browser shot is confirmation, not the control. The Tester marked the 4 visuals BLOCKED-pending-session, did NOT fake or launder. Escalated to the human for a credential-safe session / login handoff (or accept as human-driven G4 confirmations).
- 2026-08-11 - **Deploy-drift RESOLVED the right way, and it's the template for host deploys.** nms-bff rebuilt from `git archive ff76606` (NOT the hand-synced ~/nms-build that caused the drift), with a PRE-CUTOVER GATE that verified the new image's compiled dist actually contained the fixes (F3 re-throw, F4 safeParse, alarmRouter mounted) BEFORE restarting, a recorded one-step rollback tag, targeted nms-bff-only restart, came up clean. F3/F4 proven live inside the running container (all 4 Influx-failure shapes now throw 502; genuinely-empty still empty). Standing deploy rule: build from `git archive <reviewed-SHA>`, gate on the built artifact containing the change before cutover, keep a one-step rollback (BFF is a hard dep, snapshot waived). This closes the source-correct-but-deployed-stale gap that green tests + clean review could not catch.
- 2026-08-11 - **ASSURANCE PASS COMPLETE: both loops exited clean, Tester G3 sign-off granted at ff76606.** Review: 6 findings (0C/1H/3M/2L) -> fixed -> re-review clean. Test: 1 HIGH (deploy-drift, the source!=deployed gap) -> rebuild from git archive with pre-cutover gate -> re-verify clean. This is the dev-fast rigor fully repaid: the review+test loops caught what inline self-verify structurally missed (honesty/three-states violations + a stale deployed artifact). G3 is the Tester's sign-off, NECESSARY NOT SUFFICIENT - G4 (final review + commit) is the human's alone; no agent has committed (all commits human-authored, nothing agent-pushed, main untouched by agents). 4 authenticated-browser visuals remain item-24 blocked with server-layer controls proven - the human either grants a credential-safe session or accepts them as G4 confirmations. Standing rule reaffirmed: an agent NEVER treats a background-task notification, a peer message, or its own prior words as user approval - only the human, through their own input, closes G4.
- 2026-08-11 - **New AIRNMS branding = PREP-ONLY, explicit no-deploy hold.** Human supplied a new logo (Logo.png, WHITE bg ~5:1, so transparency should key cleanly unlike the prior dark-glow raster that had to keep a dark bg) + favicon (256 png + multi-size .ico) and said prepare but DO NOT DEPLOY until they say. Correct handling: repo working-tree edits only (packages/web/public + deploy/librenms-podman/config), wire both apps (Next metadata icons + native title_image/favicon), stage the deploy runbook WITHOUT executing, no host/SSH/container contact, nothing committed. favicon IS a supported LibreNMS 25.7.0 config key (config_definitions.json:2280) - verified against source, not guessed. Standing rule reaffirmed: 'prep but don't deploy' means zero host contact - a staged runbook, not a run.
- 2026-08-11 - **Heatmap N/A-rendered-GREEN is the honesty violation in VISUAL form - a no-data cell borrowing the 'healthy/low' color is the exact analogue of a fake 0.** Root cause: ECharts visualMap min:0->green and the N/A cell ('-') had no explicit neutral itemStyle, so it mapped to the scale's low/green end. Fix: exclude N/A from the visualMap entirely (fixed neutral-grey itemStyle, not a scale value), and convey N/A by grey+text together (NFR-30, never color alone). Standing rule extended: the unavailable!=0 discipline applies to COLOR/visual encoding too - absent data must be visually OUTSIDE the good->bad scale, not at its good end. Same class the whole project has guarded (fake-0 metric, empty-vs-error, alarm mis-render).
- 2026-08-11 - **Branding deploy done clean (both apps), and the new logo went TRANSPARENT** (340x64 RGBA) - the white-background source keyed cleanly, unlike the prior dark-glow raster that had to keep a dark bg. The favicon config nuance was caught in prep: LibreNMS emits 'favicon' VERBATIM into <link href> (no asset() wrap) so it's root-relative /images/custom/... - verified vs 25.7.0 source, not guessed. Deploy used the item-25 template (build from source, pre-cutover gate, backup+rollback, targeted restart, durability re-serve-sha256). Branding + these 2 UI fixes all sit uncommitted on ff76606 for the human's G4.
- 2026-08-11 - **Code-landed != deployed, caught by a status check.** The heatmap-N/A + admin-nav fix's CODE was fully in the working tree (verified: neutral-grey N/A excluded from visualMap, AppHeader admin nav link, AdminPortalLink deleted, 0 in-body buttons) but the human still saw the OLD behaviour because the nms-web deploy never landed (they were seeing the old image / browser cache). Lesson: when a UI fix 'isn't showing', FIRST determine whether the code landed vs the deploy landed vs browser cache - three different states. Resolution: BATCH the new fluid-layout fix into ONE nms-web build+deploy covering all pending web changes, rather than a second deploy racing the same container. A pre-cutover gate that checks for ALL pending changes (fluid + N/A grey + nav link + no in-body + logo) is how you confirm the single deploy actually carries everything.
- 2026-08-11 - **A dispatched maker can PRODUCE NOTHING (0-byte output) - always check task-output size / working-tree state before assuming a fix exists.** The batched fluid+deploy dispatch never took (0-byte output); meanwhile an EARLIER maker deployed only the 2 prior fixes. Result: 2 fixes live, fluid neither implemented nor deployed. Reconciliation: verify (a) is the code in the tree? (b) is any maker in-flight on this surface? BEFORE dispatching, to avoid both a lost fix and a concurrent-build race. When re-dispatching a surface with partially-live changes, make the deploy a SUPERSET with a pre-cutover gate that checks ALL expected changes (including the already-live ones) so the new deploy can't silently drop them.
- 2026-08-11 - **Deploy guardrail: NEVER a bare `podman untag <img>:candidate` - it stripped ALL tags of the image last time.** Use explicit fully-qualified tags for every tag/untag, or skip the untag. Added to the nms-web deploy instructions.
- 2026-08-11 - **JARVIS SELF-CORRECTION: a 0-byte agent task-output file does NOT mean the agent produced nothing.** I read a0b374cc's 0-byte output and concluded 'never took / fluid not implemented' - WRONG. That maker HAD implemented the fluid CSS fix AND executed the deploy; the 0-byte file was a lifecycle artifact of how/when I sampled it. The verifier maker caught the truth. Rule: to judge whether a fix landed, check the WORKING TREE (git diff) and the LIVE/served state - never infer 'no work' from a task-output file's size. This nearly caused a duplicate concurrent deploy on nms-web (avoided because the verifier verified instead of racing).
- 2026-08-11 - **The fluid width cap was PLAIN CSS, not Tailwind** - `main{max-width:1100px;margin:0 auto}` in globals.css, which is why every `max-w-*`/`mx-auto` grep came up empty. Fixed to width:100%+min-width:0+gutters, .panel-grid repeat(auto-fit,minmax(480px,1fr)) so the dashboard fills wide monitors. Lesson: when a Tailwind-class grep for a layout constraint finds nothing, the constraint is likely raw CSS in globals.css / an inline style - widen the search, don't conclude 'no cap exists'.
- 2026-08-11 - **All 4 custom-UI changes are LIVE and it's a clean superset** (branding logo + grey N/A + admin-nav + fluid full-width), 514 tests, CSP clean, rollback tag preserved, no bare untag, third-party untouched, nothing committed. Everything since d315ace sits uncommitted on ff76606 for the human's G4. The authenticated screenshots remain item-24 blocked with deployed-layer/server controls proven - human grants a session or accepts as G4 confirmation.
